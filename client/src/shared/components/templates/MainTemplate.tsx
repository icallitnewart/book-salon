import React from 'react';
import { styled } from 'styled-components';

const Container = styled.div`
	width: 100%;
`;

function MainTemplate(): JSX.Element {
	return <Container>main template</Container>;
}

export default MainTemplate;
